services:
  sumeetsaini_com:
    volumes:
      - ./services/sumeetsaini_com:/app
      - /app/node_modules
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=dev

  vulkan:
    build:
      context: ./services/vulkan
      dockerfile: Dockerfile.dev
    volumes:
      - ./services/vulkan:/app
      - ./services/vulkan/data:/app/data
      - /app/node_modules
      - /var/run/docker.sock:/var/run/docker.sock:ro
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=dev
      - PORT=3000
      - HOST=0.0.0.0
      - FROM_EMAIL=vulkan@sumeetsaini.com
      - MAIL_ENABLED=true
      - MAIN_EMAIL=dev-test@example.com
      - RELIQ_STUDIOS_EMAIL=dev-test@example.com
      - ETHEREAL_HOST=smtp.ethereal.email
      - ETHEREAL_PORT=587
      - WELL_API_KEY=dev

  arcanecodex:
    build:
      context: ./services/arcanecodex
      dockerfile: Dockerfile.dev
    volumes:
      - ./services/arcanecodex:/src
    ports:
      - "1313:1313"
    environment:
      - HUGO_ENV=development

  reliqstudios:
    build:
      context: ./services/reliq/reliqstudios
      dockerfile: Dockerfile.dev
    volumes:
      - ./services/reliq/reliqstudios:/src
    ports:
      - "1314:1314"
    environment:
      - HUGO_ENV=development

  gateway:
    build:
      context: ./services/gateway
      dockerfile: Dockerfile
    volumes:
      - ./services/gateway/nginx-dev.conf:/etc/nginx/nginx.conf:ro
      - ./services/gateway/conf.d:/etc/nginx/conf.d:ro
    ports:
      - "80:80"
    depends_on:
      - sumeetsaini_com
      - vulkan
      - arcanecodex
      - reliqstudios
